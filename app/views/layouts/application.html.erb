<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:html_title) + " - " if content_for?(:html_title)%> Top 100 Movies</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma-rtl.min.css">
  </head>

  <body>
    <section>
      <div class="container">
        <h1 class="title is-1">Top Movies from IMDB.</h1>
      </div>
      <%= form_with(url: search_movies_path, method: :get, local: true) do %>
        <div class="field is_grouped mb-5">
          <%= text_field_tag(:keywords, nil, class: "input", placeholder: "Search for movies...") %>
        </div>
        <div class="control">
          <%= submit_tag("search", class: "button is-primary") %>
        </div>
      <% end %>
      <%= yield %>
    </section>
    <% if controller_name == 'home' %>
    <P>There's no place like home.</P>
    <% else %>
  <%= render partial: 'shared/footer' %>
  <% end %>
  </body>
</html>
